/*
function showDropDown(dropDownContainer) {
  const dropDown = dropDownContainer.children[1];
  console.log(dropDownContainer.children);

  const toggleVisible = function () {
    dropDown.classList.toggle("visible");
  };

  dropDownContainer.addEventListener("mouseover", toggleVisible);
  dropDownContainer.addEventListener("mouseout", toggleVisible);
}

showDropDown(document.querySelector(".container"));
*/
